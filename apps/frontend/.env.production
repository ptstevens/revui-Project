# Production Environment Configuration
# This file is used when building for production (npm run build)

# Backend API URL
# In production, the frontend calls the API directly at api.revui.app
VITE_API_URL=https://api.revui.app/api/v1
